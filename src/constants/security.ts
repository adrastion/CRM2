/** Единые параметры безопасности (P1). */

/** Стоимость bcrypt для новых хешей паролей. */
export const BCRYPT_ROUNDS = 12;

/**
 * Нейтральное сообщение при неудачном входе
 * (не раскрывает, существует ли аккаунт).
 */
export const LOGIN_FAILED_MESSAGE = 'Неверный логин или пароль';
