/**
 * Дизайн-токены, снятые с макетов (dorabot/*.pdf, базовая ширина 1920px).
 *
 * Все значения — из макета, поэтому их не нужно «подбирать на глаз»:
 * при правках дизайна меняется только этот файл.
 */

export const colors = {
  /** Основной синий: заголовки, кнопки, активные элементы. */
  primary: '#4880FF',
  /** Тёмный синий — тень/градиент под primary. */
  primaryDark: '#0D4BD7',
  /** Светлый синий — фон ячеек календаря, заголовки таблиц. */
  primarySoft: '#BFD3FF',

  /** Фон страницы и полей ввода. */
  surface: '#F5F6FA',
  /** Фон карточек. */
  card: '#FFFFFF',

  /** Основной текст. */
  text: '#202224',
  /** Второстепенный текст, подписи полей. */
  textMuted: '#404040',
  /** Подсказки, placeholder. */
  textHint: '#8B8C8F',
  /** Мелкие пояснения под полями. */
  textFaint: '#838385',
  /** Текст пустых состояний. */
  textEmpty: '#5D5D5D',
  /** Подписи в карточках быстрых действий. */
  textSubtle: '#666666',

  /** Границы полей ввода. */
  border: '#838385',
  /** Границы неактивных полей. */
  borderDisabled: '#E3E3E3',
  /** Разделители, выделение строки в выпадающем списке. */
  divider: '#D9D9D9',
  /** Фон строк расписания. */
  rowAlt: '#EFEFEF',
  rowMuted: '#C0C0C0',

  /** Ошибка / предупреждение. */
  danger: '#F93C65',
  /** Успех, прогресс. */
  success: '#00B69B',

  white: '#FFFFFF',
  /** Затемнение фона в состоянии «ожидайте подтверждения». */
  overlay: '#D9D9D9',
} as const;

/** Радиусы из макета. */
export const radii = {
  /** Карточки авторизации (880x578, r≈35). */
  card: 24,
  /** Карточки дашборда. */
  panel: 14,
  /** Кнопки 186x80, r≈11. */
  button: 11,
  /** Поля ввода — полная пилюля. */
  pill: 999,
  /** Выпадающий список. */
  dropdown: 20,
  /** Ячейки календаря. */
  cell: 8,
} as const;

/**
 * Размеры элементов управления из макета (в px при ширине 1920).
 * На узких экранах уменьшаются через responsive-хелперы ниже.
 */
export const sizes = {
  /** Высота поля ввода: 87px внешняя рамка. */
  fieldHeight: 72,
  fieldHeightSm: 60,
  /** Кнопка 186x80. */
  buttonHeight: 64,
  buttonHeightSm: 54,
  buttonMinWidth: 186,
  /** Ширина карточки авторизации. */
  authCardWidth: 720,
  /** Ширина карточки выбора организации. */
  selectCardWidth: 1180,
  /** Ширина карточки регистрации партнёра. */
  registerCardWidth: 1400,
  /** Ширина бокового меню. */
  sidebarWidth: 288,
} as const;

/** Типографика (пересчитана из pt макета в px с сохранением иерархии). */
export const typography = {
  /** «Вход в личный кабинет» — 56pt. */
  authTitle: { xs: 30, sm: 40, md: 52 },
  /** «В аккаунт какой организации вы хотите войти?» — 45pt. */
  sectionTitle: { xs: 22, sm: 30, md: 40 },
  /** «Панель управления» — 48pt. */
  pageTitle: { xs: 26, sm: 34, md: 42 },
  /** Значения KPI — 56pt. */
  metric: { xs: 32, sm: 42, md: 52 },
  /** Заголовки панелей — 26pt. */
  panelTitle: { xs: 18, sm: 22, md: 24 },
  /** Текст в полях ввода — 33.9pt. */
  field: { xs: 17, sm: 22, md: 26 },
  /** Текст на кнопках — 35.9pt. */
  button: { xs: 17, sm: 22, md: 26 },
  /** Подписи, чекбоксы — 24.8pt. */
  label: { xs: 14, sm: 16, md: 19 },
  /** Мелкие пояснения — 16pt. */
  hint: { xs: 12, sm: 13, md: 14 },
} as const;

/** Длительности анимаций переходов между шагами авторизации. */
export const motion = {
  stepDuration: 340,
  fadeDuration: 220,
  easing: 'cubic-bezier(0.22, 0.61, 0.36, 1)',
} as const;
